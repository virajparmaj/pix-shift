{
  "name": "i-png",
  "version": "1.0.0",
  "description": "Chrome extension that converts HEIC downloads from Google Photos to PNG",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsx scripts/build.ts",
    "dev": "tsx scripts/build.ts --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "heic-to": "^1.4.2",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "tsx": "^4.0.0",
    "typescript": "^5.4.0",
    "vite": "^6.0.0"
  }
}
